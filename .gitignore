# Local Claude / MCP config
.claude/settings.local.json
# Berisi SUPABASE_ACCESS_TOKEN — jangan di-commit
.mcp.json

# Secrets / env
.env
.env.*
!.env.example
!mobile/.env.example
!mobile/.env.staging.example
!mobile/.env.production.example
*.local
# Kredensial dev — jangan di-commit
docs/kredensial-login.md
*.token
*.token.txt
cf.token*

# OS / editor
.DS_Store
Thumbs.db

# Obsidian — preferensi editor per-orang, bukan pengetahuan; seluruh vault state lokal
.obsidian/
wiki/.obsidian/

# UI/UX screenshots & mockups — artefak lokal, tidak perlu di-repo (root only; jangan match mobile/src/components/ui/)
/ui/
.gstack/
docs/screenshots/

# Session scratch (brainstorm output, throwaway uploads, layout variant sandboxes)
.superpowers/
.uploads/
performance-layout-variants/

# Supabase CLI transient state (per-machine, not source-tracked)
supabase/.branches/
supabase/.temp/
